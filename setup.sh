#!/usr/bin/sh
set -xe
apt-get install curl macchanger aircrack-ng daemonize ieee-data tmux libtext-csv-perl
# kali only
#apt-get install wordlists
#gunzip --force --keep /usr/share/wordlists/rockyou.txt.gz # entpacken
